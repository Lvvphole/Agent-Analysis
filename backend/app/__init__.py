"""Agent-Analysis backend package.

Deterministic control system around autonomous AI-generated code.

Core thesis (must never be degraded):
    AI narrative is not evidence.
    The coding agent cannot self-certify.
    Only diff + passing tests + verifier approval can decide PASS.
    No auto-merge. No auto-deploy. No direct push to protected branch.
"""

__all__ = ["constants"]
